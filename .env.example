# Supabase Configuration (для клиентского приложения)
# Эти переменные используются в браузере, поэтому безопасны для публикации

VITE_SUPABASE_URL=your_supabase_project_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key

# ⚠️ ВАЖНО: Ключи Twilio НЕ должны быть здесь!
# Ключи Twilio настраиваются в Supabase Secrets для Edge Functions:
# - Через Supabase Dashboard: Settings → Edge Functions → Secrets
# - Или через CLI: supabase secrets set TWILIO_ACCOUNT_SID=...
